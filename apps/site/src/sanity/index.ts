import { Button } from './Button';
import { SectionHeader } from './SectionHeader';
import { FeatureCard } from './FeatureCard';
import { Page } from './page'

export const sanityComponents = [Button, SectionHeader, FeatureCard, Page]