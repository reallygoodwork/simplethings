---
import { Text } from '@simplethings/react'

interface Props {
  title?: string
}

const { title } = Astro.props
---

<div class="py-2xl flex flex-col gap-l px-xl border-b border-grey-200 mb-xl">
  <!-- {
    title ? (
      <Text as="h2" level="large">
        {title}
      </Text>
    ) : null
  } -->
  <slot />
</div>
